.git
.gitignore
.DS_Store

# Local env files only. The generated .env.<project>-<env> that ENV_FILE points
# at must stay in the build context, so this cannot be a blanket .env* ignore.
.env
.env.local
.env.*.local

.vercel
coverage
dist
node_modules
npm-debug.log*
pnpm-debug.log*
yarn-error.log*
